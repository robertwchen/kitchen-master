# Experiment 003: Uncertainty Gating

## Goal
Evaluate selective decision behavior by introducing an `uncertain` gate instead of forcing overconfident line-relative classifications.

## Scope (Scaffold Stage)
- Configuration template only.
- Results artifact conventions only.
- No threshold tuning or learned confidence model yet.

## Expected Outputs
Each run should store artifacts under `results/`:
- configuration snapshot
- metrics structure (empty during scaffold stage)
- plot artifacts or plot manifest
